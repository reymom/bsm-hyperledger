package routes

import (
	"fmt"
	"net/http"
)

func generateLogisticsRoutes(mux *http.ServeMux) error {
	fmt.Println("[generateLogisticsRoutes]")
	// result, e := logisticsContract.EvaluateTransaction("CreateDelivery", "", "", "")
	// if e != nil {
	// 	return e
	// }

	return nil
}
